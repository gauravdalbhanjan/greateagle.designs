# HTTrack cache files
hts-cache/
hts-log.txt
cookies.txt
backblue.gif
fade.gif

# Video segments (too large for GitHub)
images/*.ts
images/*.m3u8
images/videokey-*

# CDN mirrors (not needed, loaded from Squarespace CDN)
images.squarespace-cdn.com/
static1.squarespace.com/
platform.twitter.com/
www.exploretock.com/
definitions.sqspcdn.com/

# Vercel
.vercel

# Dev-only inline editor server + working artifacts (not needed for deploy)
edit-server.js
artifacts/

# Personal certificate PDFs — kept private (knowledge is baked into search-data.js,
# so the AI search still answers about them without publishing the files)
assets/Certificates/

# Temp scratch files
_*.txt
_*.js

# OS
.DS_Store
Thumbs.db
